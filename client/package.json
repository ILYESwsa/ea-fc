{
  "name": "@bloxcraft/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "three": "^0.169.0",
    "zustand": "^5.0.0",
    "@bloxcraft/shared": "0.1.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "vite": "^5.4.10",
    "typescript": "^5.6.3"
  }
}
